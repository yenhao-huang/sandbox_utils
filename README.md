# Sandbox Utils

Utilities and docs for isolated sandbox workflows.

Directories:

- `codex-sandbox/`: Codex sandbox tooling and usage
- `claude-sandbox/`: Claude Code sandbox — `run.sh` launch script, `Dockerfile` for `yenhao123/claude-sandbox`
